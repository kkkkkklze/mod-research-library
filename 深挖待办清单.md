# 深挖待办清单（准备开挖）

> 用途：把"值得深挖但还没挖"的对象列成可执行的待办，每条给出**为什么挖 / 要回答什么问题 / 从哪开始读 / 体量**。
> 状态标记：`[ ]` 未挖 · `[~]` 已部分分析（有报告但没挖透）· `[x]` 已深挖
> 源码位置约定：`_teacon_decompiled/` = TeaCon 2026 的 164 个 mod；`_pack_decompiled/<包>/` = 四包补漏的 214 个 mod；`源码库/_参考仓库/`（含 `_bulk/`）= 已克隆的 591 个仓库。
> 方法：已有反编译源码的直接读；只有仓库的先 `git clone`（脚本 `研究脚本/_bulk_clone.py`）；每挖一个产出一份规范报告并登记到 `分析报告/_分析报告/`。
> 并发实测（2026-09-23）：同一宿主会话里 **3 路并行稳定**（每路一个仓库、各写一份报告）；一次性开 6 路时 5 路被「模型服务连接中断」打断（只有 1 路落盘），按 3 路一批重跑即全绿。重跑只补失败项，已落盘的报告不会被覆盖。

---

## 本轮批量进度（2026-09-23）

> 缺口盘点：`研究脚本/_missing_reports.py` → 生成 `对照表/未深挖清单.md`（有速览卡片、无深度报告的仓库，按行数降序）。
> 本轮从 **265** 个缺口挖掉 16 个，剩 **248**（其中 ≥1000 行的 180 个）。登记已同步：`分析报告/_分析报告/README-索引.md` 的 16 行「深度报告」列由 — 改为链接 + 概览计数 + `README.md` 计数。

**已挖 16 份**（统一 8 节、119~201 行、每份 68~177 处 `文件:行号`）：

| 报告 | 为什么挖 | 一句话结论 |
|---|---|---|
| `XFactHD__FramedBlocks` | #14 同题：形状×材质如何不爆炸 | 253 个 BlockType 只配 255 份 blockstate / 82 个模型；连接态不是 blockstate property，而是挂在 BlockState 对象上的两个 long 位掩码 |
| `Tower-of-Sighs__AUI` | 卡牌 GUI 的框架参考 | 等于在 MC 里重写了一个精简浏览器（手写 HTML/CSS 解析 + flexbox）；最值得抄的是两趟帧管线与「渲染前强制补交 pending 布局」 |
| `baileyholl__Ars-Nouveau` | 功法数据化 | 每个 glyph 注册即自带一份服务器 TOML（开关/费用/tier/每咒上限/非法组合）——数值平衡外包给整合包作者 |
| `klikli-dev__occultism` | 仪式玩法 | 仪式时间线只有一个 `currentTime` 标量，15 个 Ritual 子类无一覆盖 `update()`（=没有多阶段仪式）；进度只存「配方 ID + 已消耗清单」，剩余材料由全集反算 |
| `zhenshiz__ViScriptRecipe` | #15 语言进方块 | 不嵌语言：NBT 覆盖文件 + 游戏内编辑器，加载期编译成 Recipe；可抄的是「副本构建 + 原子换表 + 基线快照」这套事务骨架 |
| `Raguto__Citadel-1.21.1` | 妖兽动画引擎 | 一个 mixin 给所有 LivingEntity 挂同一条同步 COMPOUND_TAG 并自动存读；但本检出只登记 3/16 个 mixin，Tabula 动画轨解析后即丢弃 |
| `breeze-devs__Settlements-Alpha` | 蜂群/新星同题 | 原版 Brain 当宿主 + 自研 DayPlan 当调度器（只注入 2 个 behavior）；周期热路径用 `floorMod(uuid, interval)` 错峰，一行消掉整村同 tick 尖峰 |
| `MagicHarp__confluence` | Colossus 的 BOSS 样本 | **boss 内核在缺失的 git submodule 里**（本检出只有主模块 ConfluenceOtherworld），招式调度/血条只能标未验证；外围可抄 KillBoard 的「enum 单例 + Codec/StreamCodec 成对 + 全表快照包」，另附三条反例 |
| `Low-Drag-MC__KilaGraph` | 阵图/回路节点化 | `PreparedGraph` 用一次预解析换零运行期查找；它划的界线要一起抄——可以优化寻址，不可以优化求值次序（惰性拉输入是可观察语义） |
| `Minecraft-LightLand__ModularGolems` | 傀儡 + 1.20.1 基础设施 | 它不做方块结构识别：把「拼结构」外包给原版配方系统（自定义 RecipeSerializer），免费拿到 datapack 覆盖 / JEI / 进度 / `/recipe`，零网络代码 |
| `SilentChaos512__Silent-Gear` | 法宝 = 材料 × 部件 | construction（材质规格）与 properties（算好的属性）分离 + 三趟汇总 + 结果烘回物品、读取即命中；真实版本是 1.21.1 NeoForge（卡片写的 1.20.1 错） |
| `jinqinxixi__Trinkets-and-Baubles-Forge-1.20.1` | 1.20.1 槽位系统 | 槽位/GUI/穿戴全外包给 Curios，本仓库只是内容包；真正可抄的是 `ICapabilitySerializable` 直转发的落盘最短路径，外加一串静默失效的反面教材 |
| `FINDERFEED__FDLib` | Colossus HUD 层 | bar 靠 UUID + entityId(-1) 脱离实体；「两个 int 的旁路事件通道」一套协议服务所有血条表现；`AttackChain`（stage 推进 + NBT 续招）近乎零依赖可整包搬 |
| `TelepathicGrunt__Bumblezone` | multiloader 工程范本（读者两边都写） | 这仓库**没有 Forge 侧**（`settings.gradle:22` 只 include common/fabric/neoforge）；共享靠"common 的 java 被两个 loader 各编译一遍"，不是 Architectury 转换层；`ModuleHolder(id, codec, factory)` 让 common 完全不碰存储 API，各 loader 各自落成 AttachmentType，"死亡是否复制"是声明参数。附带纠正：它也没有天气系统（grep weather=0）、不用 NightConfig（NeoForge 用自带 ModConfigSpec，Fabric 用 MidnightLib） |
| `MysticMods__Roots` | 灵力/可再生资源循环 | 「自然之力」Grove Power **没有全局池、没有 SavedData**：每个 Grove Stone 方块实体每 tick 从周围方块现算瞬时功率，消费者用 `PowerTicket` 按 grove tag 分账撮合；限频三闸门全在数据侧（同类方块计数上限 / 对径形状匹配 / rank 倍率）。仪式时间线仍只是 `lifetime` 单标量——与 Occultism 同样**没有阶段机**，差异在失败语义与自动续期 |
| `TeamPneumatic__pnc-repressurized` | 蜂群行为系统 + 殖民物流的重工业版 | 一份 `IProgWidget` 只写一对编解码器（`byNameCodec().dispatch` + `ByteBufCodecs.registry().dispatch`）就同时进 BE NBT / 物品数据组件 / 双向 GUI 同步包 / 可分享 JSON——照这个形状写行为表就不存在"序列化写三遍"；无人机另有 `TICK_RATE=3` 降频、ChunkCache 快照 + TTL 认领、`@DescSynced`+BitSet 脏位。压缩气**没有全局图**，是每 handler 本地扩散 + `BlockCapabilityCache` 邻居缓存 |

**顺带纠正的既有事实**：confluence 的 boss 内核不在此检出（git submodule 未取回）；Bumblezone 无 Forge 侧、无天气系统、不用 NightConfig；索引里 occultism 的项目名写作「Occultism KubeJS」但这版并无 KubeJS compat；Silent-Gear 卡片的「1.20.1 userdev」错误（实为 NeoForge 21.1.219）；Trinkets-and-Baubles 卡片把它当独立槽位系统（实为 Curios 内容包）；速览卡片的行数普遍比实测偏高（最多差值 = 文件数，末行换行符口径），13 份报告各自在第 2 节标注了复核值与稀疏检出缺什么。

**下一批候选**（≥1000 行、与在用工程直接同题、仍只有卡片）：`refinedmods__refinedstorage2`、`Railcraft__Railcraft`、`Draconic-Inc__Draconic-Evolution`、`Geforce132__SecurityCraft`（CameraFeed 已被两份设计文档引用）、`breezeth-CN__OrdertoCook`、`Lightman314__LightmansCurrency`（新星殖民地的经济对照）、`ARxyt__ColonyPathingEdition`（1.20.1 殖民寻路）、`CyclopsMC__*` 一族（IntegratedDynamics/Terminals/Tunnels + CyclopsCore）、`CoFH__CoFHCore`、`The-Aether-Team__The-Aether`、`KODU16__vsie`、`Ellpeck__ActuallyAdditions`；另有 #14 的 Chisel Reborn + CTM（不在 591 仓库里，需先用 `_bulk_clone.py` 补克隆）。完整 248 项见 `对照表/未深挖清单.md`。

---

## 第一梯队：与你的方向直接相关（优先挖）

### 1. `[x]` Domum Ornamentum —— 方块版"材质拼接" ⭐ 已挖（2026-09-20）
> **报告**：[`分析报告/_分析报告/深挖__Domum-Ornamentum__方块版材质拼接.md`](./分析报告/_分析报告/深挖__Domum-Ornamentum__方块版材质拼接.md)（10 节，全部结论带 `文件:行号`）
> **五个问题的答案**：① 材质 = `record MaterialTextureData(Map<部件id, Block>)`，装在 `domum_ornamentum:texture_data` DataComponent（persistent + networkSynchronized），物品/方块实体/存档/渲染四处共用一份；② `MateriallyTexturedBakedModel` 是**每次渲染按材质重建**的包装模型（双 Guava 缓存，键 = `record(材质, 渲染类型)`，10k/2min），`RetexturedBakedModelBuilder` 用 **sprite 名 = 部件 id** 命中并做 UV 归一化重映射，源模型不支持该渲染类型时**擦除**该部件；③ 上色不查表：**tint 打包 `Block.getId(state)<<8 \| tintIndex`** → 色处理器 `>>8` 还原源方块状态再问原版 `BlockColors`，生物群系/液体/染料染色原生生效；④ `retainComponentsFromBlock` 只留目标方块声明过的部件，掉落/复制走 `BE.saveToItem`→`collectImplicitComponents`→DataComponent；⑤ 与 47 类型结合给了三条路线（A 造 47 个代表方块 / B 只抄渲染层吃现有贴图 / C 只抄数据形状），**建议先 C，手感不足再补 A 的"行为委托四件套"**，并列出三个陷阱（id 占用 `minecraft:block/*` 命名空间、部件顺序即槽位语义、烘焙模型数量上限）。

- **为什么**：你研究过 Tinkers/Tetra/Silent Gear 的物品材质拼接，这是**建筑方块**的同一问题，而且做法最优雅：材质不存颜色，而存"**源方块引用**"。
- **要回答**：① `MaterialTextureData`（数据组件 `{部件id → Block}`）如何随物品/方块/NBT 流转；② `MateriallyTexturedBakedModel` + `RetexturedBakedModelBuilder` 怎么用源方块的贴图**重建**烘焙模型（缓存键是什么、什么时候失效）；③ `MateriallyTexturedBlockColor` 上色与"源方块自身颜色逻辑"的关系；④ `retainComponentsFromBlock` 换形保材质的实现；⑤ 与我的 47 类型调色板管线怎么结合（源方块 = 一个"材质包"，可不可以让 47 个类型各造一个"代表方块"当素材）。
- **起点**：`_pack_decompiled/All the Mods 10 - ATM10/domum-ornamentum-1.0.234-snapshot-main/com/ldtteam/domumornamentum/{block,client/model,client/color,api}`
- **体量**：231 java
- **同族一起看**：`BlockUI`（同团队 UI 框架）、`Stylecolonies`（殖民地建筑风格）、`Multi-Piston`

### 2. `[~]` 车万女仆 2.0 的 LLM agent 框架
- **为什么**：你已经知道它"有 LLM 聊天"，但没挖透**接口设计**；这是"自然语言指挥游戏实体"的现成骨架。
- **要回答**：① `ai/agent/context/` 的上下文注册与组装（`GameContextRegister`、各 `*MaidContexts` 的贡献方式）；② `ai/agent/tool/` 的 function-calling 工具签名与执行（`QueryGameContextTool`、`SwitchWorkTaskTool` 等，如何把模型输出映射到实体状态变更）；③ `ai/agent/skill/` 的 YAML schema（技能怎么定义、条件、参数）；④ 异步与限流（`LLMSite.LLM_HTTP_CLIENT`、失败兜底）；⑤ 安全边界：哪些工具是"只读"、哪些"可写"，有无服务端校验。
- **起点**：`_teacon_decompiled/touhoulittlemaid-2.0.0-neoforge+mc26.1.2-all/com/github/tartaricacid/touhoulittlemaid/ai/`
- **体量**：138 java（agent 25 / manager 24 / service 89）

### 3. `[~]` Minecraft Mod MCP —— 把游戏变成 agent 可操作环境
- **为什么**：直接对上你的 agent 工作流，是"无头验证"缺的那一环；也是现成的"游戏内自动化"骨架。
- **要回答**：① JSON-RPC 方法表全貌与 `enumerate_widgets` / `call_screen_method` / `get_screen_buttons` 的反射实现细节；② `ReflectedInputHandler` + `RenderThreadExecutor` 的线程调度；③ 截图链路（`glReadPixels` → `BufferedImage` → 缓存）；④ SSE 通道与并发会话；⑤ **改造评估**：把它的 HTTP 骨架抽出来，接到我自己的 mod 测试流程（启动游戏 → 打开我的 GUI → 点按钮 → 截图断言）。
- **起点**：`_teacon_decompiled/minecraft-moddev-mcp-neoforge-26.1.2-0.2.0/xyz/langyo/minecraft/mcp/`
- **体量**：20 java / 1.07 万行（**建议全读**）

### 4. `[ ]` Mahou Tsukai 的魔法回路（`block/circuits`）
- **为什么**：著名魔法 mod，把法术做成**可编排的回路**——对"功法/阵图/符箓"的数据化是直接参考。
- **要回答**：① 回路的表示与求值（方块网络？图？）；② 法术参数/触发方式的数据化程度；③ 与粒子/音效表现的耦合；④ 能否借它的形状做"灵力回路"。
- **起点**：`_pack_decompiled/All the Mods 10 - ATM10/mahoutsukai-1.21.1-v1.36.27/stepsword/mahoutsukai/block/circuits`
- **体量**：6.8 万行（整包）

### 5. `[ ]` AnvilCraft 的行为树与配方族
- **为什么**：`api/behavior/BehaviorTree<T>` 是"机器/实体行为"的可组合实现；1498 个配方（`anvil_collision/*`）是"玩法即数据"的极端案例。
- **要回答**：① 行为树节点类型与执行上下文（`TreeNode`/`ExecutableTreeNode`/`PredicateTreeNode`/`SetTreeNode`/`ExecutionContext`）；② `anvil_collision` 配方族的 schema 与"铁砧等级 × 材料"的判定；③ 流体/能量网络三件套（Scanner/Manager/Valve）的同步与性能；④ 1566 个成就怎么组织。
- **起点**：`_teacon_decompiled/anvilcraft-neoforge-26.1.2-1.6.0+build.374/dev/dubhe/anvilcraft/{api/behavior,api/fluid/network,api/energy,recipe}`
- **体量**：2094 java / 19.8 万行（建议按子系统分次挖）

### 6. `[ ]` synaxis（控制学）—— "控制椅"操控系统
- **为什么**：1078 文件 / 12.7 万行的中文 mod，`api/controlchair`（+event/raw 分层）看起来是"坐进椅子操控装置/载具"，对"御剑 / 操控傀儡 / 架势"类玩法有参考。
- **要回答**：① 控制椅的输入→装置控制的数据流；② API 的两层（event / raw）设计意图；③ 网络同步（操控指令如何跨端）；④ 与载具/物理 mod 的接口。
- **起点**：`_pack_decompiled/璇穹之歌/控制学synaxis-1.5.0/com/verr1/synaxis/api/controlchair`

---

## 第二梯队：工程与表现技法

### 7. `[~]` NeoMTR 的三件套（脚本渲染 / 内嵌 Web / 批渲染）
- **要回答**：① Rhino 脚本驱动资源包渲染的沙箱与生命周期（`render/scripting/{ScriptHolder,ScriptResourceUtil}`）；② Jetty + tprobe3 的 Web 前端如何暴露游戏数据（数据面板范式）；③ `sowcer` 批渲染的批次/材质/shader 组织（大量模型时的解）。
- **起点**：`_teacon_decompiled/NeoMTR-300.6.1+26.1.2/{cn/zbx1425/mtrsteamloco,cn/zbx1425/sowcer,com/lx862/tprobe3}`

### 8. `[ ]` BlockUI + LDLib2 —— MC 内的现代 UI 框架对照
- **为什么**：你已经分析过 LDLib2（NeoForge 侧）；`BlockUI` 是 ldtteam 的同类框架，两者对照能看清"游戏内编辑器/仪表盘"的通用做法。
- **起点**：`_pack_decompiled/All the Mods 10 - ATM10/blockui-1.0.211-1.21.1-snapshot`；对照 `源码库/_参考仓库/_bulk/Low-Drag-MC__LDLib2` 与已写报告。

### 9. `[ ]` 粒子系统三方对照：AAA Particles vs ParticleStorm vs dcvfx
- **为什么**：你手里已经有三种粒子实现（TeaCon 的 AAA Particles/AAA Particles: World、ParticleStorm、BleedZone7 的 dcvfx），做一次横向对照能定下"自己该用哪种/抄哪段"。
- **要回答**：数据驱动程度、网格生成方式、批渲染、光照/sprite 处理、性能取舍。
- **起点**：`_teacon_decompiled/{aaa_particles-neoforge-26.1.2-2.2.4-alpha.1,aaa_particles_world-neoforge-26.1.2-2.0.1-alpha.4}` + 已写的 ParticleStorm / BleedZone7 报告。

### 10. `[ ]` Create 物理/航空学生态的多人同步与渲染
- **为什么**：你有 `魔术枪械`/Create 附属方向；这一族（`sable`、`simulated`、`createpropulsion`、`simulatedcoasters`、`sable_xaero_bridge`）解决的是"物理载具在多人下的同步与渲染"。
- **起点**：`_pack_decompiled/璇穹之歌/{过山车simulatedcoasters-0.1.5,地图看到物理装置sable_xaero_bridge-0.1.11}`；仓库 `ryanhcode/sable`、`Creators-of-Aeronautics/Simulated-Project`（已克隆）。

### 11. `[ ]` 大型内容 mod 的工程规模对照
- `Modern-Industrialization`（AztechMC，5.5 万行）、`Immersive Energistics`（IE×AE2 桥）、`IceAndFire CE + Uranus`（6.3 万行）、`unusualend`（5.2 万行）、`block_factorys_bosses`（5.2 万行）、`cataclysm_dimension`（11.6 万行）。
- **要回答**：它们各自的"新增内容成本"是多少（数据驱动 vs 代码），跨 mod 集成怎么写（IE×AE2 是范例）。

---

## 第三梯队：素材与待筛

### 12. `[x]` 剩余整合包的 mod 清单 → 候选已筛出（2026-09-20 完成）
- **15 个包 / 4478 个 jar 已全部索引**，逐包清单在 `对照表/Mod清单__<包>.md`（Infinity Legacy II 505 / 乌托邦探险之旅 451 / SDBF 360 / Immersive Fight 305 / 涟漪之篇 291 / Sky Energy Tech 284 / 你好新蒸程 279 / ea 278 / new age sky 278 / New Age Science and Technology 263 / 通天之路 259 / NovaEngineering-World 258 / 星罗棋布 241 / 龙之冒险 235 / 愚者 191）。
- 元数据里带仓库的 **2323 个**（52%）；其中**不在已有 591/164 映射里的 152 个** → `对照表/剩余包_新仓库清单.md`（已标注 12 个误匹配 / 24 个深挖候选）。
- 只有非 GitHub 地址（CurseForge/Modrinth/官网，可继续定位）的 **524 个** → `对照表/剩余包_非GitHub地址mod工作单.md`。
- 顺带修好一个索引缺陷：**1.12.2 老包用 `mcmod.info` 而非 `mods.toml`**，补上解析后 NovaEngineering-World 从「3 个带仓库」涨到 37 个、包内 224/258 拿到了 mod id。
- **筛出的候选已并入下方 #14–#17**（第一/二梯队新增），其余标 ★ 的留在 `rest_new_repos.md` 备查。
- 过程脚本：`研究脚本/_index_packs.py`（索引）、`annotate_new_repos.py`（候选标注）、`downloader.py`（多镜像下载+SHA512 校验）。

### 13. `[ ]` 待补的边角源码（3 个）
- 璇穹之歌：`橱柜库.jar`（本地 jar 名与元数据都对不上）
- ATM10：`ATO - All the Ores`、`Clean Swing Through Grass`
- 处理办法：PCL 手动下载 → 丢进 `Downloads/_teacon/downloaded/`（下载缓存，可用 `研究脚本/_downloader.py` 重建） → 我跑 `decompile_downloaded.py` 补上。

---

## 新候选（2026-09-20 从 15 包 4478 jar 里筛出）

### 14. `[ ]` 方块材质拼接第二条路线：Chisel Reborn + CTM ⭐ 与 #1 一起挖
- **为什么**：#1 Domum Ornamentum 是"源方块引用"路线，Chisel 是"变体 + 连接材质（CTM）"路线；你在美术管线里已经吃过 `_e` 发光约定和方块贴图的坑，这两条路线的取舍直接决定你 47 类型方块化时怎么写。
- **要回答**：① `Chisel-Reborn`（`matthewperiut/Chisel-Reborn`）的变体注册与模型生成；② `ConnectedTexturesMod`（`Chisel-Team/ConnectedTexturesMod`）的 CTM 图集/tick 逻辑与 1.21 的 sprite 体系怎么接；③ CTM 与"材质拼接"能否组合。
- **起点**：`源码库/_参考仓库/_bulk/matthewperiut__Chisel-Reborn`、`Chisel-Team__ConnectedTexturesMod`（如未克隆，用 `_bulk_clone.py` 补）

### 15. `[~]` 三个"把语言/脚本做进方块"的工程范式（横向对照）
- **本轮进度**：已挖 1/3 —— ViScriptRecipe（报告 `分析报告/_分析报告/zhenshiz__ViScriptRecipe.md`）。意外结论：它**不嵌脚本语言**，`.recipe` 是 NBT 覆盖文件 + 游戏内可视化编辑器，加载期编译成正版 Recipe；与 Hex Casting（栈式 VM）/ NeoMTR（Rhino）的分界判据已写进该报告第 8 节。剩余 2/3：SFM、CC:Tweaked（Carpet 亦未挖）。
- **为什么**：你已经有了 Hex Casting 的栈式 VM 与 NeoMTR 的内嵌 Rhino；再有三个就能把"脚本沙箱"这件事定型。
- **要回答**：① **SFM**（`TeamDman/SuperFactoryManager`）：自研语言的词法/语法/编译器/解释器 + 物流执行，**编译错误怎么反馈给玩家**；② **CC: Tweaked**（`cc-tweaked/CC-Tweaked`）：Lua 运行时、多机网络、持久化、权限与配额；③ **Carpet**（`gnembon/fabric-carpet`）：`scarpet` 语言 + `/carpet test` 测试框架（**无头可复现验证的成熟做法**，直接对得上你的四门）。横向产出"语言进 MC 的检查表"。

### 16. `[ ]` 数据附着系统对照：Cardinal Components API（Fabric） vs 我的 `AspectProfile`
- **为什么**：`_pack_decompiled`/local 侧你已实现能力序列化（存档 schema 2、`hasSavableContent()` 守卫）；Cardinal Components 是 Fabric 生态**事实标准**的同一问题解，能验证你的形状选择，也能查出你没考虑的情形（跨存档迁移、同步策略、懒加载）。
- **要回答**：① 注册/附着/查询三段式 API；② 同步策略（自动/手动/从不）与 `ComponentSyncMode`；③ 迁移与版本化；④ 与 Forge/NeoForge capability 的语义差异（你两边都写）。

### 17. `[ ]` 纯结构/群系 datapack 当 mod 发：Dungeons and Taverns / Moog's 结构族
- **为什么**：你刚把**面与结构搬进数据包**（`faces/` `structures/`、存档 schema 2），这批 mod 是"结构集合"的规模化案例（如何组织几百个结构 + 结构集 + 群系绑定），能给你一份"规模化的目录/命名/引用规范"。
- **要回答**：① 结构与结构集的目录组织与命名；② 生成规则（间距/分离/生物群系标签）怎么表达；③ 反查它们怎么处理"同一结构在多群系"的引用。

### 18. `[ ]` 表现层补充：Entity Texture Features（发光贴图实现侧）+ Player Animator
- **为什么**：`Traben-0/EmissiveMod`（ETF）是你美术管线里 **`_e` 发光约定的实现侧** —— 命名/打包/性能/与光影的交互全在里面；`KosmX/minecraftPlayerAnimator` 是玩家模型动画的第三条路线（对照 GeckoLib 与 BleedZone7 的 dcanim）。
- **要回答**：① ETF 的贴图发现约定（`_e`、随机、眨眼）与 atlas/`RenderType` 处理；② 与 Sodium/光影的兼容写法；③ Player Animator 的数据来源（文件格式）与网络同步。

---

## 施工顺序建议

1. ~~**Domum Ornamentum**（材质拼接）~~ ✅ 已完成 → 接 **#14 Chisel Reborn + CTM**
2. **Chisel Reborn + CTM**（#14，同题的第二条路线，和 #1 一起挖出取舍结论）
3. **Mod MCP 改造评估**（能立刻改善你的开发/测试流程）
4. **TLM2 的 LLM agent 框架**（NPC 自然语言指挥）
5. **Mahou Tsukai 回路** + **synaxis 控制椅**（修仙玩法机制）
6. **AnvilCraft 行为树/配方族**（模块化工业体系）
7. **语言进方块三方对照**（#15，含 Carpet 的 `/carpet test` —— 与你的四门验证同题）
8. 第二梯队按兴趣挑（粒子三方对照、Create 物理族、NeoMTR 三件套、BlockUI）
9. 第三梯队：Cardinal Components（#16）、结构 datapack 族（#17）、ETF/Player Animator（#18）、边角源码补漏（#13）

> 若要恢复"多 agent 并行挖掘"：在 ZCode 设置里给当前模型选择思考档位（当前报错 `reasoning-level-missing`），之后我可以每个 mod 派一个 agent 并行产出报告。
